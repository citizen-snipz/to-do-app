<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To-Do App</title>
    <link rel="stylesheet" href="style.css">
    <script
      src="https://kit.fontawesome.com/935416031f.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <main class="container">
      <h1>To Do List</h1>
      <form action="/addTodo" method="post">
        <input class="search" type="text" placeholder="Task" name="todoInput" />
        <input class="button" type="submit" value="Add Task" />
      </form>
      <ul class="todoList">
        <% for (let i = 0; i < info.length; i++) { %>

        <li class="todoItem"> 
            <label for="checkbox"> 
                <input type="checkbox" <%= info[i].completed && 'checked' %> >
                <span class=<%= info[i].completed && 'complete' %>><%= info[i].todo %></span>
                <span class="far fa-trash-alt"></span>
            </label>
        </li>
        <% } %>
      </ul>
    </main>
    
    <script src="/main.js"></script>
  </body>
</html>
